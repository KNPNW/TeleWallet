import Foundation
import UIKit

public enum Theme {
    public enum Colors {
        public static let background = UIColor.blue
    }

    public enum Fonts {
        public static let balance = UIFont.boldSystemFont(ofSize: 35)
        public static let walletButton = UIFont.monospacedSystemFont(ofSize: 12, weight: .regular)
    }
}
